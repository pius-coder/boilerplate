# Step 01: Analyze

**Task:** Temps SDK and environment exact acceptance from 02d
**Started:** 2026-08-13T12:18:39Z

---

## Context Discovery

⚡ ECONOMY MODE - direct tools, no subagents

### Preflight checks
- Protected hashes (02A list, `.codex/output/apex/02-temps-standalone-web/protected-hashes.initial`):
  - `src/db/schema.ts` `af3b912f…68f10` ✓
  - `src/lib/auth.ts` `8f6ba4bd…70f0c0` ✓
  - `src/db/migrations/meta/_journal.json` `c87947eb…5ed6` ✓
  - `src/db/migrations/meta/0030_snapshot.json` `1fb04bcb…30764` ✓
  - `src/db/migrations/meta/0031_snapshot.json` `e70a3321…2b0a` ✓
  - `bun.lock` `2d2b3836…24b5f` ✓ (unchanged — install must only touch package.json + pnpm-lock.yaml)
- Branch: `feat/african-regional-foundations` (not main) ✓
- Previous missions 01–04 completed; 02e/02f not yet started (no provider mounting in this mission).

### Registry state
- `pnpm view @temps-sdk/react-analytics dist-tags.latest version --json` → `{"dist-tags.latest": "0.0.4", "version": "0.0.4"}`
- Install exact `@temps-sdk/react-analytics@0.0.4` (no caret) via `pnpm add --save-exact`.
- Official docs `https://temps.sh/docs/react-analytics-sdk` served compressed/unreadable (same as mission 01 noted); prop contract verified from installed `.d.ts` types (mission step 2).

### Files to touch
| File | Current state | Change required |
|------|---------------|-----------------|
| `package.json` | no temps deps (line 44-81) | add `@temps-sdk/react-analytics: 0.0.4` exact |
| `pnpm-lock.yaml` | no temps entries (grep count 0) | regenerated by pnpm add |
| `src/lib/env.ts` | `RawEnvSchema` (line 150-289), `envBoolean` helper (line 54-85) | add `NEXT_PUBLIC_TEMPS_ANALYTICS_ENABLED: envBoolean(false)`; GA+Temps conflict check in `validateAppEnv()` (line 608-660) |
| `src/config/analytics.ts` | `hasAnalytics()` = GA only (line 15-17), `hasConsentGatedScripts()` (line 27-28) | add `hasTempsAnalytics()` (literal `process.env.NEXT_PUBLIC_TEMPS_ANALYTICS_ENABLED` + trim/lowercase truthy set 1/true/yes/on); `hasAnalytics()` true if GA OR Temps; `hasConsentGatedScripts()` unchanged semantics |
| `.env.example` | "Analytics & Ads" block (line 169-173) | add commented, disabled Temps flag |
| `tests/unit/architecture.test.ts` | public-secret rules exist (SENTRY/OTEL checks?) | add forbidden `NEXT_PUBLIC_SENTRY_DSN`, `NEXT_PUBLIC_TEMPS_API_TOKEN`, `NEXT_PUBLIC_TEMPS_API_URL`, `NEXT_PUBLIC_OTEL_*` |
| `tests/unit/env.test.ts` | ENV_KEYS list (line 6-69), boolean tests | add Temps flag tests (default false, true/false values, invalid rejected, GA+Temps conflict rejected) |
| `tests/unit/…` | no analytics config test file exists | new pure tests for `hasTempsAnalytics`/`hasAnalytics`/consent-gated scripts |

### Existing patterns
- `envBoolean(false)` used for every optional public flag (`NEXT_PUBLIC_CAPTCHA_ENABLED: envBoolean(true)` line 181, `NEXT_PUBLIC_LOCALE_DETECTION: envBoolean(false)` line 168).
- `env.ts` conflict pattern: production-only checks in `getForbiddenProductionEnv`/`getInvalidProductionEnv`; GA+Temps conflict must be a config error in `validateAppEnv` (likely always active, not only production).
- `analytics.ts` literal `process.env.NEXT_PUBLIC_*` access "written out literally because Next inlines these at build time by static substitution" (line 10-13).
- `TRUE_VALUES = ["1","true","yes","on"]` in env.ts mirrors the mission's required `hasTempsAnalytics` truthy set.
- Architecture test reads files as text; new public-secret rule fits the "site content" or "conventions" describe block.
- `tests/unit/consent.test.ts` tests `isAllowed`; consent-gated scripts test will assert `hasConsentGatedScripts()`.

## Inferred Acceptance Criteria
- [ ] AC1: `@temps-sdk/react-analytics@0.0.4` (exact, dist-tags.latest) in dependencies; types confirm `TempsAnalyticsProvider` accepts `basePath`, `disabled`, `ignoreLocalhost`, `enableSessionRecording`.
- [ ] AC2: `NEXT_PUBLIC_TEMPS_ANALYTICS_ENABLED` in `src/lib/env.ts` via `envBoolean(false)`, never required in production.
- [ ] AC3: `hasTempsAnalytics()` literal-env truthy check (1/true/yes/on); `hasAnalytics()` = GA OR Temps; `hasConsentGatedScripts()` unchanged behavior.
- [ ] AC4: `validateAppEnv()` rejects GA+Temps together (mutually exclusive).
- [ ] AC5: `.env.example` has commented, disabled Temps variable.
- [ ] AC6: architecture test forbids `NEXT_PUBLIC_SENTRY_DSN`, `NEXT_PUBLIC_TEMPS_API_TOKEN`, `NEXT_PUBLIC_TEMPS_API_URL`, `NEXT_PUBLIC_OTEL_*`.
- [ ] AC7: only `package.json` + `pnpm-lock.yaml` are dependency files modified; `bun.lock` hash intact; no provider mounted; `CONSENT_VERSION` untouched.
