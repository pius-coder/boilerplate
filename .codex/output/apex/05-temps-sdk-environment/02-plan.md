# Step 02: Plan

**Task:** Temps SDK and environment exact acceptance from 02d
**Started:** 2026-08-13T12:18:39Z

---

## Planning Progress

## Implementation Plan: Temps SDK + environment contract (02D)

### Overview
Install `@temps-sdk/react-analytics` at the exact published `latest` (0.0.4), confirm the provider prop contract from the installed `.d.ts`, then wire the environment contract: optional public flag (default false, mutually exclusive with GA), analytics config helpers, and the architecture guard against public Temps/Sentry/OTEL secrets. No provider mounting, no `CONSENT_VERSION` change.

### Order of operations
1. Install dependency (package.json + pnpm-lock.yaml only; bun.lock untouched).
2. Verify types (blocking gate for the mission).
3. env.ts schema + conflict.
4. analytics.ts helpers.
5. .env.example.
6. Architecture test rule.
7. Tests (env + pure analytics config).
8. Full validation.

---

### File Changes

#### `package.json` (dependencies block, line 44-81)
- `pnpm add --save-exact @temps-sdk/react-analytics@0.0.4` — exact `"0.0.4"`, no caret. Do NOT touch `bun.lock` (protected hash `2d2b3836…24b5f` must remain).

#### `pnpm-lock.yaml`
- Regenerated by pnpm add — expected and required; the only other lock/dependency file allowed to change.

#### `src/lib/env.ts`
- Add to `RawEnvSchema` (~line 280, near `NEXT_PUBLIC_GOOGLE_ANALYTICS_ID`): `NEXT_PUBLIC_TEMPS_ANALYTICS_ENABLED: envBoolean(false)` — optional, default false, never required in production.
- In `validateAppEnv()` (line 608): add a config-error check — if `env.NEXT_PUBLIC_TEMPS_ANALYTICS_ENABLED` and `env.NEXT_PUBLIC_GOOGLE_ANALYTICS_ID` are both set, push a descriptive issue to a new `invalid`/config section ("mutually exclusive; Temps is the recommended target"). This check must be active in all runtimes (not only production) since it is a config contradiction, but follow existing style: production-only checks live in get*ProductionEnv helpers; this one is not production-gated. Decide: add a small `getInvalidConfigEnv(raw)` used always, or inline in validateAppEnv. Inline in validateAppEnv is simplest and matches "config error" intent.

#### `src/config/analytics.ts`
- Add `hasTempsAnalytics()`: returns true when `process.env.NEXT_PUBLIC_TEMPS_ANALYTICS_ENABLED` (accessed literally) is truthy after trim/lowercase in the set `1|true|yes|on`. Implement as `const raw = process.env.NEXT_PUBLIC_TEMPS_ANALYTICS_ENABLED; return raw !== undefined && ["1","true","yes","on"].includes(raw.trim().toLowerCase())`. Keep the existing comment about literal env access for Next inlining (lines 10-13).
- Update `hasAnalytics()`: `hasTempsAnalytics() || Boolean(process.env.NEXT_PUBLIC_GOOGLE_ANALYTICS_ID)`.
- `hasConsentGatedScripts()`: unchanged body — still `hasAnalytics() || hasAdvertising()`.

#### `.env.example` (Analytics & Ads block, lines 169-173)
- Add commented, disabled entry: `# NEXT_PUBLIC_TEMPS_ANALYTICS_ENABLED=false` with a short comment explaining: first-party Temps analytics; mutually exclusive with NEXT_PUBLIC_GOOGLE_ANALYTICS_ID (boot fails if both set); enabled value forms 1/true/yes/on.

#### `tests/unit/architecture.test.ts`
- In the "conventions" describe (or a new small describe): forbid public secrets — regex match on `NEXT_PUBLIC_SENTRY_DSN`, `NEXT_PUBLIC_TEMPS_API_TOKEN`, `NEXT_PUBLIC_TEMPS_API_URL`, and `NEXT_PUBLIC_OTEL_` across all `FILES` (source tree). Also scan `apps/admin/**` source files (consistent with other admin-scanned rules) since env.example/lock aren't source. Report offending paths.

#### `tests/unit/env.test.ts`
- Add `NEXT_PUBLIC_TEMPS_ANALYTICS_ENABLED` to `ENV_KEYS` list (line 6-69).
- Tests:
  1. default false outside production (add assertion to the "uses local defaults" test or a new test).
  2. accepts true forms (1/true/yes/on incl. whitespace/case) → parsed true.
  3. accepts false forms (0/false/no/off) → parsed false.
  4. rejects invalid value (e.g. "enabled") with env issue.
  5. rejects GA+Temps conflict in any runtime: set `NEXT_PUBLIC_GOOGLE_ANALYTICS_ID` + Temps enabled → `EnvValidationError` containing both names.
  6. never required in production: `setProductionEnv()` passes without the Temps variable.

#### `tests/unit/analytics-config.test.ts` (NEW FILE)
- Pure tests for `src/config/analytics.ts`:
  - `hasTempsAnalytics()`: false when unset/empty; true for `1`, `true`, `yes`, `on`, mixed case and whitespace; false for `0`, `false`, `no`, `off`, garbage.
  - `hasAnalytics()`: true when GA set; true when Temps set; true when both; false when neither.
  - `hasConsentGatedScripts()`: true when analytics (GA or Temps) set; true when adcode set; false when none.

---

### Testing Strategy
- Extend `tests/unit/env.test.ts` (schema + conflict).
- New `tests/unit/analytics-config.test.ts` (pure helpers).
- Extend `tests/unit/architecture.test.ts` (public secrets).

### Acceptance Criteria Mapping
- AC1: package.json + lock + type verification in execute.
- AC2: env.ts schema line.
- AC3: analytics.ts helpers.
- AC4: env.ts conflict check.
- AC5: .env.example.
- AC6: architecture.test.ts.
- AC7: validation step — `git diff --check`, only package.json + pnpm-lock.yaml as lock/dep files, bun.lock hash intact, no src/providers/* changes, no CONSENT_VERSION change.

### Risks & Considerations
- `pnpm add` may rewrite bun.lock? No — pnpm never writes bun.lock; verify hash after install.
- Do NOT run `pnpm install` with bun; packageManager is pnpm@10.22.0.
- Next inlining: `hasTempsAnalytics` must access `process.env.NEXT_PUBLIC_TEMPS_ANALYTICS_ENABLED` literally — no computed access (comment at analytics.ts:10-13).
- GA+Temps conflict message must be descriptive and include both variable names.

---
## Step Complete
**Status:** ✓ Complete
**Files planned:** 6 modified + 1 new test file
**Tests planned:** 3 files (env extended, analytics-config new, architecture extended)
**Next:** step-03-execute.md
