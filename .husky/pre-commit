#!/usr/bin/env sh
# Fast gate: lint + the mocked test tiers, ~10s. A full `bun run build` here took
# long enough that it invited --no-verify, which skipped the tests riding on it.
# CI still runs lint, tests, and both builds on every push.
bun run lint && bun run test:fast
